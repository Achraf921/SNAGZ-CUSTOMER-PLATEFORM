import React from "react";
import AccountsManager from "./AccountsManager";

const AdminAccounts = () => {
  return (
    <AccountsManager type="admin" title="Gestion des Comptes Administrateur" />
  );
};

export default AdminAccounts;
